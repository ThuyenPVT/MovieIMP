import 'package:flutter/material.dart';

final bookmarkIcon = const Icon(
  Icons.bookmark,
);

final menuIcon = const Icon(
  Icons.menu,
  color: Colors.white,
);

final bookmarkIconRed = const Icon(
  Icons.bookmark,
  color: Colors.red,
);

final bookmarkIconWhite = const Icon(
  Icons.bookmark,
  color: Colors.white,
);

final airplayIcon = const Icon(
  Icons.airplay,
);

final starIcon = const Icon(
  Icons.stars,
);

final exitIcon = const Icon(
  Icons.exit_to_app,
);

final starIconWhite = const Icon(
  Icons.star,
  color: Colors.white,
);

final shareIcon = const Icon(
  Icons.share,
);

final starIcon15 = const Icon(
  Icons.star,
  size: 15.0,
);

final searchIcon = const Icon(
  Icons.search,
);
